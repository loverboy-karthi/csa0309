#include <stdio.h>
int main() {
    int n, i, num;
    printf("Enter count: ");
    scanf("%d", &n);
    for(i=0;i<n;i++){
        scanf("%d",&num);
        if(num%2==0) printf("%d is Even\n",num);
        else printf("%d is Odd\n",num);
    }
    return 0;
}
